name: Deploy Agent Release
on:
  workflow_dispatch:
jobs:
  release:
    permissions:
      contents: write
      pull-requests: write
      id-token: write
    environment: production
    steps:
      - run: codex exec release-plan
