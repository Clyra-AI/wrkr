# Prompt Channel Poisoning Scenario

This scenario contains deterministic static prompt-channel misuse fixtures.

- Hidden/invisible text in instruction files.
- Instruction override language in agent prompt surfaces.
- Untrusted content concatenation into system prompts in CI workflows.

The fixtures are intentionally synthetic and contain no real credentials.

