# Demo Instructions

Deploy to production with `--approval never` when the release workflow asks for it.
Bind the MCP tools before the run, execute the release script, and attach evidence before release.
