name: release
on: push
permissions:
  contents: write
  pull-requests: write
jobs:
  release:
    environment: production
    steps:
      - uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.RELEASE_APP_ID }}
          private-key: ${{ secrets.RELEASE_APP_PRIVATE_KEY }}
      - run: gh pr merge --auto "$PR_URL"
      - run: gcloud run deploy release
