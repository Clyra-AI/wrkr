interface:
  display_name: "Backlog Implement"
  short_description: "Execute plan stories with required test gates"
  default_prompt: "Use $backlog-implement to execute a full plan or a selected epic with required validation and DoD checks."
