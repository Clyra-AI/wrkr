# wrkr version

## Synopsis

```bash
wrkr version [--json]
wrkr --version [--json]
```

## Contract

- Human output prints `wrkr <version>`.
- JSON output emits `status` and `version`.
- Exit code is `0` on success.
